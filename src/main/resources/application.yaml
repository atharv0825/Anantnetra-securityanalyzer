spring:
  application:
    name: anantnetra

  datasource:
    url: jdbc:mysql://localhost:3306/security_analyzer
    username: root
    password: Atharv@0825

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        format_sql: true

server:
  port: 8080